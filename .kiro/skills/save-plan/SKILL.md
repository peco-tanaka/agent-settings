---
name: save-plan
description: 計画（plan）をmarkdownファイルとして .kiro/plans/ に保存・管理する。最も典型的なトリガーは、計画提示後のユーザーからの「y」という短い承認応答である。以下の場合に発動する：(1) 計画を提示した後にユーザーが短く承認したとき（「y」「ok」「問題ない」など。特に「y」は最頻出トリガー）、(2) ユーザーが計画の保存を明示的に求めたとき（「planを保存して」「計画をファイルに残して」など）、(3) 実装中にplanの更新・修正を求められたとき（「planを更新して」「計画を修正して」など）。計画のファイル保存・更新に関するすべてのリクエストに対応する。
---

# Save Plan - 計画ファイル保存スキル

計画（plan）を `.kiro/plans/` ディレクトリにmarkdownファイルとして保存・更新する。

## ワークフロー

### Step 1: ディレクトリの準備

`.kiro/plans/` ディレクトリが存在しない場合、作成する。

```bash
mkdir -p .kiro/plans
```

### Step 2: ファイル名の決定

```text
.kiro/plans/YYYY-MM-DD_task-name.md
```

- `YYYY-MM-DD`: 当日の日付
- `task-name`: タスクの概要をkebab-caseで簡潔に表現（英語、3-5語程度）
- 例: `.kiro/plans/2026-02-01_add-user-authentication.md`
- 同名ファイルが存在する場合、末尾に連番を付与する（例: `_2`）

### Step 3: planの保存

planの全文をそのままmarkdownファイルとして書き出す。見出し・リスト・コードブロックなどのフォーマットはそのまま維持する。

### Step 4: 保存完了の報告と実装開始の確認

保存したファイルパスをユーザーに報告し、このまま実装を開始するか確認する。

- ユーザーが実装開始を希望した場合: planの内容に従って実装を開始する
- ユーザーが実装を保留した場合: そのまま終了する

### 既存planの更新

実装中にplanの内容が変更された場合、`.kiro/plans/` 内の該当ファイルを最新内容で上書きする。変更前の内容はgitで追跡可能なため、ファイル内に変更履歴は残さない。

## 重要なルール

- planの内容は一切改変せず、そのまま保存する
- `.gitignore` への `.kiro/plans/` の追加はユーザーの判断に任せる（自動追加しない）
